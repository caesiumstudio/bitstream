<resources>

    <style name="Theme.BitStream" parent="@style/Theme.Leanback">
        <item name="android:windowBackground">@color/fastlane_background</item>
        <item name="android:colorBackground">@color/fastlane_background</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

</resources>